# frozen_string_literal: true

RSpec.describe Betamax do
  it "has a version number" do
    expect(Betamax::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
